# Feature Brief

Add a small command-line workflow that helps a developer move from a short
feature idea to a reviewed implementation plan and a first test-driven slice.

The workflow should keep the skill as guidance only. It must not grant tools,
change policy, approve actions, alter provider settings, or write files by
itself.
