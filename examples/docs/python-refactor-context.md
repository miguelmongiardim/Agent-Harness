# Python Refactor Context

The bundled Python refactor scenario is intentionally small. The mock model
must inspect the target file and use the observed function body before
proposing a patch. The task id alone is insufficient authority for an action.
