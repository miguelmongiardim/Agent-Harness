# Semantic Note

Settings ingestion governance cleanup is the semantic-only example. The local
dense fixture maps those words to the requested maintenance topic while lexical
matching should not see the original query terms here.
