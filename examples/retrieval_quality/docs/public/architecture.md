# Retrieval Architecture

The refactor config loader policy path is the canonical public architecture
example for this demo. It should appear as both lexical and dense evidence in
hybrid retrieval because the query terms are exact.
