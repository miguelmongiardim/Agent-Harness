# Provider Audit Notes

This workflow keeps provider access explicit by using a mock provider profile in the generated config and a policy that requires approval for hosted providers.

Use the generated task and eval examples as local fixtures for audit evidence review.
