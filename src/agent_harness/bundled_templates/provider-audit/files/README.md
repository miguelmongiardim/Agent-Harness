# Provider Audit Workflow

Generated scaffold for a local provider audit workflow using a mock provider profile and Agent Harness examples.

Run `agent-harness task validate examples/provider-audit.task.json` from the generated project to validate the task contract.
