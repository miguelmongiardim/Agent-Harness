# Example CLI Tool

Generated scaffold for a Python command-line application with an argparse entry point, tests, and Agent Harness workflow examples.

Run `python -m pytest` from the generated project to validate the CLI behavior.
