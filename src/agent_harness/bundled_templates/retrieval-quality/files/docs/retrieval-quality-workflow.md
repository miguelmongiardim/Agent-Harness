# Retrieval Quality Notes

This workflow keeps retrieval local by default. The generated config uses lexical retrieval and deterministic local settings.

Use the generated knowledge-base note as a small fixture for retrieval experiments.
