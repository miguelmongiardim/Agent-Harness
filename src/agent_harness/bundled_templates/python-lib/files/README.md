# Example Python Library

Generated scaffold for a local Python package using a `src/` layout, pytest, and Agent Harness workflow examples.

Run `python -m pytest` from the generated project to validate the library.
