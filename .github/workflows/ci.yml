name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: Ruff
        run: python -m ruff check src tests

  typecheck:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: Mypy
        run: python -m mypy

  unit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: Unit tests
        run: >
          python -m pytest
          tests/test_schemas.py
          tests/test_policy.py
          tests/test_mock_model.py
          tests/test_retrieval.py
          tests/test_tools.py

  integration:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: Integration tests
        run: >
          python -m pytest
          tests/test_cli.py
          tests/test_runtime.py
          tests/test_storage_audit.py
          tests/test_phase0_walking_skeleton.py
          tests/test_phase5_context_retrieval.py
      - name: Compileall
        run: python -m compileall -q src tests

  adversarial:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: Adversarial tests
        run: >
          python -m pytest
          tests/test_phase3_policy_denied_data.py
          tests/test_phase4_approval_bound_tools.py
          tests/test_phase7_evals_exports.py

  e2e:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dev dependencies
        run: python -m pip install -e .[dev]
      - name: End-to-end tests
        run: >
          python -m pytest
          tests/test_phase6_runtime_resume.py
          tests/test_e2e_cli_lifecycle.py
